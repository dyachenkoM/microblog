[supervisord]
nodaemon=true

[program:backend]
command=uv run uvicorn src.main:app --host 0.0.0.0 --port 8000
directory=/app
autostart=true

[program:nginx]
command=nginx -g "daemon off;"
autostart=true